{
  "name": "mite-assistant-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticated network MCP server scaffold for Claude Code",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "latest",
    "@modelcontextprotocol/server": "latest",
    "zod": "latest"
  }
}
